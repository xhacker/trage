# -*- coding: utf-8 -*-

import os

trage_dir = os.path.join(os.getenv("HOME"), ".trage")
prob_root_dir = os.path.join(trage_dir, "problem")
code_dir = os.path.join(trage_dir, "code")
db_location = os.path.join(trage_dir, "trage.db")
tmp_dir = '/tmp/'

root_password = '我爸是陶陶'
